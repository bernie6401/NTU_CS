a = 1 // 1 ; b = '''

const fs = require('fs');

fs.readFile("./flag", 'utf8',(error, data) => {
    if (error) {
        console.error(error);
        return;
    }
    console.log(data.split('\n')[0]);
})

/* '''

f = open("flag", "r")
print(f.read().split('\n')[0])
# */
